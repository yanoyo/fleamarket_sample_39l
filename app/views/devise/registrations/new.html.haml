.user-new-registration
  = render partial: "shared/new-header"
  %main.single-main
    %section.l-single-container
      %h2.l-single-head
        会員情報入力

      .l-single-inner
      = form_for(@user, url: registration_path(resource_name)) do |f|
        = devise_error_messages!
        .l-single-content
          .form-group
            = f.label :nickname do
              ニックネーム
            %span.form-require
              必須
            = f.text_field :nickname, autoforcus: true, placeholder: "例) メルカリ太郎", class: "input-default"
          .form-group
            = f.label :email do
              メールアドレス
            %span.form-require
              必須
            = f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "PC・携帯どちらでも可", class: "input-default"
          .form-group
            = f.label :password do
              パスワード
            %span.form-require
              必須
            = f.password_field :password, autocomplete: "new-password", placeholder: "6文字以上", class: "input-default"
          .form-group
            = f.label :password do
              パスワード(確認)
            %span.form-require
              必須
            = f.password_field :password_confirmation, autocomplete: "new-password", placeholder: "6文字以上", class: "input-default"
          .form-group
            .l-chapter-sub-head
              本人確認
            .l-single-text
              安心・安全にご利用いただくために、お客さまの本人情報の登録にご協力ください。他のお客さまに公開されることはありません。

          = f.fields_for :profile, @user.profile || Profile.new do |user_profile|

            .form-group
              = user_profile.label :family_name do
                姓 (全角)
              %span.form-require
                必須
              = user_profile.text_field :family_name, autocomplete: "family_name", placeholder: "例) 山田", class: "input-default"

            .form-group
              = user_profile.label :first_name do
                名 (全角)
              %span.form-require
                必須
              = user_profile.text_field :first_name, autocomplete: "first_name", placeholder: "例) 彩", class: "input-default"

            .form-group
              = user_profile.label :family_name_kana do
                姓カナ (全角)
              %span.form-require
                必須
              = user_profile.text_field :family_name_kana, autocomplete: "family_name_kana", placeholder: "例) ヤマダ", class: "input-default"

            .form-group
              = user_profile.label :first_name_kana do
                名カナ (全角)
              %span.form-require
                必須
              = user_profile.text_field :first_name_kana, autocomplete: "first_name_kana", placeholder: "例) アヤ", class: "input-default"

            .form-group
              = user_profile.label :"生年月日" do
                生年月日
              %span.form-require
                必須
              %br

              .birthday-select-wrap.select-wrap
                = user_profile.select :birth_year, options_for_select(1900..2019), {prompt: "--"}, {autofocus: 'true', class: 'select-default'}
                %i.fas.fa-angle-down
                %span 年

              .birthday-select-wrap.select-wrap
                = user_profile.select :birth_month, options_for_select(1..12), {prompt: "--"}, {autofocus: 'true', class: 'select-default'}
                %i.fas.fa-angle-down
                %span 月

              .birthday-select-wrap.select-wrap
                = user_profile.select :birth_day, options_for_select(1..31), {prompt: "--"}, {autofocus: 'true', class: 'select-default'}
                %i.fas.fa-angle-down
                %span 日

          .form-info-text
            ※ 本人情報は正しく入力してください。会員登録後、修正するにはお時間を頂く場合があります。

          .form-group
            .l-single-text__text-center
              「次へ進む」のボタンを押すことにより、
              =link_to "利用規約", "https://www.mercari.com/jp/tos_list/"
              に同意したものとみなします

          .form-group
            = f.submit "次へ進む", class: "form-group__btn-default form-group__btn-red "

          .form-group
            .form-group__text-right
              =link_to "本人情報の登録について", "https://www.mercari.com/jp/help_center/article/423/"
              %i.fas.fa-angle-right

  = render partial: "shared/new-footer"

